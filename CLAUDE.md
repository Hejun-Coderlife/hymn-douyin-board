# 赫眉 43 家门店抖音拍摄脚本看板

纯静态 HTML 网站，无后端，最终部署到 GitHub Pages。所有计算在浏览器里完成。

## 背景

- 43 家门店，每家店一个抖音商家号；另有品牌号「赫眉美妆护肤连锁」（抖音号 `74892160771`），**不进看板，导入时忽略**。
- AI 给每家店生成拍摄脚本，**每店每天 1 条**（2026-09-16 从「每周 2 条」改成按天，颗粒度就是天）。
- 脚本排到**一整年**（生成窗口 = 往前 2 周 + 往后 52 周）。
- 看板有两种视图，靠顶部**月份选择条**切换：
  - **月视图**（默认，定位在本月）：该月每天一列，格子=当天那条脚本的状态；
  - **全年总览**：每月一列，格子里是该店该月的完成情况条 + `已发/已到期`，点一下进那个月。
- 店员发视频**不手动打卡**，靠导入抖音来客导出的视频列表**自动匹配**。
- 不做逐条视频审核，看板上能点到已发布视频的抖音链接即可。

## 目录

```
index.html            总部看板（电脑为主）。**没有顶栏**（用户要求去掉），
                      左侧栏 = logo + 4 个面板导航 + 数据状态 + 导入按钮。
                      面板：脚本日历 / 门店列表 / 视频数据导入 / 效果统计
store.html            门店页（手机为主，只读），?store=<抖音号>。
                      **2026-09-17 砍到只剩清单**：头部只有 logo + 眉批 + 店名
                      （抖音号/每天1条/往后13周、「本机没有视频数据」那两行副标题，以及
                      「本周期脚本 / 今天之后」KPI 区，用户明确说不要，别加回去）；
                      每天一行只有 **日期（周几）+ 标题 + 完成情况**，
                      标签/痛点/开头/分镜/封面/要点全收进 `.detail`，点整张卡片展开。
                      展开态 `.card2.open` = **浅灰底 `--paper` 通栏（负边距铺满）+ 左边 2px 黑竖条 +
                      上下 `--line-2` 线**，收起的行是白底；里面原本也用 `--paper` 的块
                      （`.bigline`/`.tgline`/`.pill.todo`/`.hashrow span`）要翻成白，否则糊成一片。
                      行右边有**方框「展开 / 收起」按钮**（`.tog`，展开后黑底白字），是唯一的展开提示。
                      详情里**没有「专属标签」块**（2026-09-17 先是收进话题标签，当天又整个取消）。
                      话题标签支持复制（专属标签已取消，只剩普通话题标签）：
                      **标题行有「复制全部标签」按钮**，单个标签点一下复制自己；
                      `copyText()` 在 `navigator.clipboard` 不可用时退回 `execCommand`（file:// 下常被禁）。
                      痛点那块小标题叫「拍给谁看」，里面是 人群(`audience`) + 她的烦恼(`pain`)
                      —— 原来叫「说给谁听」且只放 pain，用户看不懂以为是标题。
                      **今天那张卡片**有普蓝「今天」角标 + 左侧竖线（`.card2.istoday`，2026-09-20 加）：
                      店员点开就是来找今天拍什么的。
                      详情最下面有整行的**「复制整条脚本」**（`scriptText()` 拼纯文本：
                      标题 / 开头 3 秒 / 逐幕画面+台词 / 结尾 / 封面 / 要点 / 避坑 / 标签），
                      手上只有一部手机，拷进备忘录照着念最省事。
assets/style.css      视觉（后台控制台风格，见「视觉」一节）
assets/core.js        数据加载、日历/周次计算、匹配引擎、localStorage 读写
assets/board.js       总部看板逻辑（日历大表 / 筛选 / 侧栏 / 导入 / 统计）
assets/store.js       门店页逻辑
assets/logo-ink.png   深色 logo（浅底用）
assets/logo-white.png 白色 logo 原件（深底备用）
data/stores.json      门店档案（数据源，见 tools/build_stores.py）
data/scripts.json     脚本数据（数据源）
data/stores.js        由 json 自动包出来的 js（页面实际加载的就是它）
data/scripts.js       同上；改完 json 跑 python3 tools/sync_data.py
vendor/xlsx.full.min.js  SheetJS，本地内置（不走 CDN，保证离线/墙内可用）
tools/build_stores.py    从 xlsx 重建 stores.json
tools/gen_sample_scripts.py  生成示例脚本占位数据
tools/content_lib.py         脚本素材库（12 个内容方向 × 8 种拍摄形式的分镜骨架）
tools/sync_data.py           data/*.json -> data/*.js（上面两个脚本结尾会自动调）
reference/            视觉参考 B-浅米系.html、样例 xlsx（不部署也无妨）
格子样本.html          日历格子的四种画法并排对比（挑样式用，不影响看板）
```

## 视觉

**画风对标爱马仕官网 `https://www.hermes.cn/cn/zh/`（用户 2026-09-17 指定）。
在此之前的奶油珍珠 + 香槟金 + 毛玻璃 + 大圆角那一套已整体作废，旧版在 git 里（改动前最后一次提交 `15f99eb`）。**

**下面这些色值是 2026-09-17 从用户截的 hermes.cn 真实页面上逐点取样出来的，别凭印象改：**

| token | 值 | 说明 |
|---|---|---|
| `--bg` | `#FCF7F1` | 页面底：暖奶油。**坑**：真站截图上量到的是 `#FDF5EF`，但 CSS 写 `#FDF5EF` 在用户显示器上截出来是 `#FEF3ED`（偏粉 2 阶）——显示器色彩配置的转换。所以这里按实测差值补偿过，**别照截图取样改回 `#FDF5EF`**。校色方法：让用户把 hermes.cn 和本看板各截一张全窗口图，量两者底色像素，按差值调 |
| `--card` | `#FFFFFF` | 数据区白块（KPI / 卡片 / 日历表 / 抽屉），**无边框无阴影**，靠白 vs 奶油的对比分区 |
| `--paper` | `#F2EDE7` | 表头 / hover / 次级块 |
| `--tile` | `#E2DDD6` | 真站商品图底色，备用 |
| `--t1` | `#000000` | 正文和标题是**纯黑** |
| `--t3` | `#5E5E5E` | 次级文字（取自面包屑） |
| `--line-2` | `#D2CFCD` | 分区线（实测） |
| ~~`--brand`~~ | ~~`#F26E21`~~ | **橙色已全部弃用**（用户 2026-09-17：「我不喜欢橙色」）。变量还在但值改成了 `#4A423A`，
别再往页面上加任何橙 |
| 状态各一色 | 待拍 `#E8E3DC`、已发布 `#2F5D45` 墨绿、逾期 `#9C3B2E` 砖红 | 用户 2026-09-17 从四个方案里选的。
之前试过「全灰阶」被否（「全是黑白太单调」） |
| `--navy` | `#2C4A6E` | 今天那列 / 本周 / 链接 / 自由发挥小点 |

排版（第一版这里也错过，记牢）：
- **全站无衬线**（`PingFang SC`）。真站上没有衬线字，标题是**加粗黑体**，不是「细而宽的宋体」。
  `--serif` 变量还在，但已经指向 `--sans`，只为兼容旧写法。
- **字距按正常来**（`--track:.06em`，中文标题 0~.01em）。**不要**把中文拉成 `.14em~.34em` 的稀疏字距，那是臆想出来的。
- 标题层级抄真站的「男士 / 2026秋冬系列」：上面一行 12px 灰面包屑，下面 22px 加粗黑标题，再来一条 `#D2CFCD` 分割线。
- 链接是黑字 + 1px 下划线，不是彩色字。

组件约定：
- **直角、无圆角、无阴影、无渐变、无毛玻璃、无纸纹**（`--radius`/`--shadow` 值是 `0`/`none`，别往回加）。唯一的阴影是 `.modal`。
- **按钮**：透明底 + 1px 黑边 + 12.5px 字，hover 整块填黑；`.btn.primary` 是黑底白字。
- **左侧栏**：奶油底 + 右侧一条分区线，选中态 = **左边 2px 黑竖条 + 加粗**（UI 强调色是黑不是橙）。
  **可折叠**（2026-09-17 加，对标 ChatGPT）：`#navTog` 是个 `position:fixed` 的方块小按钮，
  展开时贴侧栏右上角、收起后落在图标栏正中（只挪 `left`，不重复两个按钮）。
  **收起 ≠ 整条藏起来**：第一版做成 `translateX(-100%)`，按钮飘到页面左上角把标题挡住了，用户当场否了。
  现在收起 = `body.navhide` → 侧栏宽度收到 `--nav-w-rail`（58px）的**图标栏**：
  文字 `.lb`、分组标题、logo、数据状态、底部说明全隐藏，只剩 4 个图标 + 一个 ↥ 导入图标，
  `.main` 的 `margin-left` 跟着收到 58px。导航 `<a>` 上有 `title`，鼠标停住能看名字。
  状态存 `localStorage.hymn_navhide`，下次打开保持。窄屏（侧栏变横条）时按钮 `display:none`。
- **月份条**：**没有贯穿整行的长横线**（用户明确要求去掉），只有每个月份自己的线：选中 = 2px 黑，hover = 1px 滑线；
  线是**整个按钮那么宽**（比文字宽），月份按钮 `flex:1 1 0` 等分撑满整行，**右边不留空白**，字号 14px。
- **hover 滑线**（抄 hermes.cn 顶部导航，用户 2026-09-17 录屏指定）：1px、文字下方约 5px、展开 `.26s cubic-bezier(.25,.1,.25,1)`，
  **从左长出、移开向右收**（靠两个 `transform-origin`：hover 时 `left`，默认 `right`）。
  已用在：左侧导航（文字包了 `<span class="lb">`）、月份条 `.mchip`、门店页链接 `.mob`。
  （门店页的「看完整分镜 `.more`」2026-09-17 已删掉，改成点整张卡片展开；CSS 还留着没人用。）
  通用类是 `.uline`。**方框按钮 `.btn` 不用这个**，它们是 hover 填黑（真站 CTA 就是填充）。
- **日历格子**：13px 直角小方块，**一律实心、没有描边**（用户反馈「方框线太粗、黑色也不好看」）：
  待拍浅灰 / 已发布墨绿 / 逾期砖红，右上角 5px 普蓝圆点 = 当天另有自由发挥。
  （驼黄 `--guess` 是「当周推测」那档留下的，规则删了色值还在 CSS 里，没人用。）
  想换画法先看 `格子样本.html`（方块 / 小圆点 / 细竖条 / 高对比四种并排，同一份假数据）。
  **图例色块写在 index.html 内联样式里，改色要同步。**
  **悬停提示是自己画的**（`.celltip`，2026-09-20 从原生 `title` 换掉，理由跟图块区一样：
  原生的要等 1 秒、字还小）：格子上写 `data-tip`（里面带 `\n`，CSS `white-space:pre-line` 分行），
  `bindBoardTip()` 在 `.gridwrap` 上挂**一次** mouseover 做事件委托 —— 几千个格子别一个个绑。
  浮层 `position:fixed`（表是横向滚动的），顶到屏幕上沿会翻到下面去。
- 品牌线（珀莱雅等）标签用灰。
- **分镜序号不要实心方块**（用户 2026-09-17：「大黑色方块…颜色太重」）：`.shot .n` 是右对齐的灰数字，
  台词块靠左边一条 1px 引导线分层。整体原则——**实心深色块只留给日历格子的状态**，其它地方别用。
- **全年总览的计数要用 `S.monthAgg`**（`recompute()` 里一次扫完，键是 `门店|年月`）。
  原来每个格子都 `S.scripts.forEach` 扫一遍全量脚本 —— 43 店 × 14 月 × 1.6 万条 ≈ 1000 万次，
  翻到全年总览就卡。**别再在格子里遍历 `S.scripts`。**
- 门店列表表格：第 1、2 列有 `min-width`，不然窄列会被挤成竖排（踩过）。
  第一列是**编号**（行号 + 下面一行灰色的 `S01` 门店编号，2026-09-17 加）。

**图块区（`#tiles`，2026-09-17 加）**：对着 hermes.cn 首页那排灰底商品图做的，四块 4:3 灰底方块 + 块下小字标签：
1. **完成率圆环**（当前视图口径，中间直接放数字）
2. **近 13 周发布量**（单序列柱状，只标峰值和本周，悬停看每周）
3. **状态构成**（一根堆叠条，段间留 2px 缝，下面四个直接标注带数值）
4. **今天的活**（相机 + 补光灯线描，`stroke-dasharray` 一笔一笔画出来）

- 数据全部现算（`renderTiles()` 在 `render()` 里跟 KPI 一起刷），口径跟 KPI 一致。
- 配色沿用状态色，跑过 dataviz 校验：CVD 分离 ΔE 17.7、正常视觉 20.5、对比度全过；
  唯一 FAIL 的「彩度偏低」是**有意保留**（爱马仕色系要克制），靠每块都带文字标签做二次编码兜底。
- **类名坑**：`.hero` 已经被旧的深色横幅占用（背景 `#16130f`），图块里的大数字用的是 `.tval`，别再用 `.hero`。
- **别给 `.tiles .tile` 设默认 `opacity:0`**：万一 `.booted` 没加上，图块就永远不见了。过场层本来就盖着页面。

**刷新过场**：`#boot` 全屏奶油底 + 居中 logo，logo 从左往右揭出来（clip-path）、下面再滑出一条线；
`core.js` 的 `HY.bootDone()` 在数据渲染完后淡出，另有 3 秒保险丝，出错也不会把页面盖死。
**时长 2026-09-17 砍过一半**（用户嫌等太久）：揭字 .38s、划线 .3s、淡出 .25s、`BOOT_MIN` 420ms，
整套约 0.7s（原来是 .85s/.7s/.55s + 850ms ≈ 1.4s）。`BOOT_MIN` 别低于 420，否则揭字会被切一半。
真正的数据加载其实只占几十毫秒，等待感基本来自这个硬性最短时长。
**踩过的坑**：`store.js` 一直没调 `HY.bootDone()`，门店页每次都在等那条 3 秒保险丝
（用户 2026-09-17：「感觉有 3 秒」）。**新页面写完记得在渲染完、以及每条 return/catch 上都调一次**；
实测现在门店页过场 720ms 消失。
对应 hermes.cn 刷新时居中那匹马。index.html 和 store.html 都有。

**背景动效（2026-09-17 重做）**：现在是**纯 CSS**——`body.fxon::before/::after` 两团暖色径向渐变，
`transform` 缓慢漂移（26s / 34s，`prefers-reduced-motion` 下停），右上角按钮开关，状态照旧存
`localStorage.hymn_bg3d`。`assets/bg3d.js` 还在但**页面已不再引用**。

**为什么换掉 WebGL —— 真正的坑（查了很久）**：
`html` 和 `body` 当时**两层都写了不透明底色**。body 的背景是普通盒子背景，会**盖住 `z-index:-1` 的
背景层**，所以无论是 bg3d 的画布还是 CSS 层，画得再亮也看不见（用户连着说了三次「看不出来有动效」）。
**修法：底色只留在 `html` 上，`body{background:transparent}`，别再给 body 加底色。**
顺带记两条排查经验：① 判断动效有没有生效，读像素/比两帧差异，别靠眼睛；
② 无头 Chrome 在这个项目上截图和 WebGL 都不可靠（连 CSS 底色都截不出来），验证要以真实浏览器为准。

另外 `bg3d.js` 的帧循环本来还有个 bug（首帧被 `now - last < FRAME` 吞掉），已经顺手修了：
`start()` 先无条件画一帧，节流时间戳取不到就退回 `Date.now()`。

## 数据

### data/stores.json

从 xlsx 的「抖音号 / 抖音号名称」提取。**`douyinId`（抖音号）是门店唯一标识，任何地方都不要用店名匹配。**

```json
{
  "douyinId": "70299081510",
  "accountName": "赫眉(西坞店)",
  "storeName": "西坞店",
  "brandLine": "",            // 珀莱雅 / 自然堂 / 华悦美妆 / 美妆星空 / 美时百货，无则空
  "region": "", "storeType": "", "customer": "",
  "mainService": "", "scenes": [], "onCamera": "",
  "startDate": null,          // 新账号起始日，只有三横店 = 2026-08-24
  "note": ""
}
```

店名写法不统一（`赫眉(西坞店)`、`赫眉·珀莱雅(新河店)`），解析规则：取括号内为 `storeName`，`赫眉·` 与括号之间为 `brandLine`。

区域/门店类型只从店名里推能确定的（带行政区前缀、带商场字样），其余**一律留空不瞎猜**。

**门店档案可以在页面上直接改**：「门店列表」面板的表格每格都能编辑，改动存
`localStorage.hymn_store_edits_v1`，加载时覆盖到 stores 上，看板立刻生效（筛选器会自动出新选项）。
要固化进项目就点「导出 stores.json」，把文件放回 `data/` 覆盖，再跑 `python3 tools/sync_data.py`。

### data/scripts.json

**每店每天 1 条**。`id` = `门店编号-日期`（如 `S01-2026-09-14`）。

```json
{
  "id": "S01-2026-09-14",
  "tag": "#赫眉0315",          // 【已废弃】原来的专属标签，2026-09-17 起页面不显示、匹配不用
  "store": "抖音号",
  "date": "2026-09-14",        // 计划发布日期
  "topic": "抗初老",            // 内容方向
  "format": "前后对比",          // 拍摄形式
  "title": "", "titles": ["备选标题"],
  "audience": "", "pain": "", "cause": "", "promise": "", "service": "",
  "hook": "开头3秒",
  "shots": [{"scene": "画面", "line": "台词", "sec": 3}],
  "duration": "30-40 秒",
  "cover": "封面怎么做",
  "bgm": "", "props": [],
  "cta": "结尾引导",
  "tips": ["拍摄要点"], "avoid": ["避坑"],
  "hashtags": ["#赫眉0315", "#抗初老", "..."]
}
```

**两个显示层的坑（2026-09-20 修，两个页面都有一份）**：
- `quote()`：素材里的台词本身就可能带 `「」`（如 `「{objection}」`），外面别再套一层，
  否则显示成 `「「…」」`。开头已经是 `「` 的就原样用。
- `ctaText()`：占位数据的 `cta` 自带「结尾引导：」前缀，页面上方本来就有同名小标题，会重复念一遍。
  `gen_sample_scripts.py` 已经不写这个前缀了，显示层再剥一次，老数据也不会重复。

**器材前提（2026-09-17 用户纠正：「我们店员没有专业道具」）**：店员手上只有**一部手机**。
`content_lib.py` 里所有分镜/要点都必须是「一部手机 + 店里现成的东西（护理床、镜子、毛巾、产品、
热喷机、窗边自然光）」能拍的。**禁止出现**：补光灯、三脚架、稳定器、皮肤检测仪 / 水分测试笔 /
紫外线相机、白板手绘板、以及要剪辑软件才能做的事（降饱和、延时、快剪卡点）。
要放慢 / 加字幕 / 分屏，统一写「用抖音自带的」。改完记得重跑 `python3 tools/gen_sample_scripts.py 43`。

现在是 `tools/gen_sample_scripts.py` + `tools/content_lib.py` 生成的**全量 43 店**占位数据
（43 × 每天 1 条 × 15 周 = 4515 条）。真脚本由 AI 生成后替换 `data/scripts.json` 再跑 sync 即可。

**分片（重要）**：整年 16254 条完整脚本约 35MB，绝不能一次加载，所以拆成两层——
- `data/scripts-index.js`（2.0MB，全量加载）：每条只有 id/store/date/topic/format/tag，
  日历、状态计算、完成率、全年总览全靠它；
- `data/scripts/<门店编号>-<年月>.js`（602 个，单片中位 67KB，**按需加载**）：完整分镜等详情。
  点开侧栏时 `HY.loadDetail(code, ym)` 注入 `<script>` 拉取
  （file:// 下 fetch 不行但 script 标签可以）。切到「门店×月份」这么细，是因为按门店整份有 700KB+，手机端吃不消。

**加数据字段时注意**：日历上要用的字段必须加进 `INDEX_FIELDS`（见 tools/sync_data.py），
否则索引里没有，看板取不到。

## 视频数据导入

- 在总部看板里上传抖音来客导出的 xlsx，**用 SheetJS 在浏览器本地解析，不上传任何服务器**。
- 按「视频ID」去重合并，**累积不覆盖**（每次导出只含一部分视频）。
- 合并结果存 `localStorage`（key `hymn_videos_v1`），并提供**导出备份 / 导入备份 JSON** 按钮，防清缓存丢数据。
- 导入时丢弃品牌号 `74892160771` 的视频。

### xlsx 真实列（2026-09-16 样例，共 19 列 3055 行）

```
数据日期范围 | 视频ID | 跳转链接 | 抖音号 | 视频作者类型 | 抖音号名称 | 视频标题 |
视频发布时间 | 视频直接成交金额 | 视频种草价值(元) | 视频总成交价值(元) |
引流直播间成交金额(元) | 视频直接成交券数 | 视频播放次数 | 视频有效播放次数 |
视频观看成交转化率 | 成交Top1商品id | 成交Top1商品价格 | 成交Top1商品名称
```

已核实的样例事实：
- 44 个抖音号 = 43 门店 + 1 品牌号。`视频作者类型` 全是「商家」，区分不了品牌号，只能按抖音号排除。
- 空标题 230 条 = **7.53%**，只能走兜底匹配。
- 「数据日期范围」= `20260817~20260915`（统计范围），但 `视频发布时间` 跨 `2026/05/20 ~ 2026/09/15`。**统计范围 ≠ 发布时间范围。**
- 播放为 0 的 830 条里，绝大多数发布时间早于统计范围（范围内仅 41 条为 0）→ 佐证「播放数只统计范围内」。
- `视频ID` 是 19 位数字，**必须按字符串处理**，否则 JS 精度丢失。
- `视频发布时间` 格式 `2026/08/25 18:39`（字符串）。

## 匹配规则

**脚本专属标签（`tag`）2026-09-17 整个取消了。** 原来的设计是店员发布时把 `#赫眉0315`
打进抖音标题，看板扫标题做精确匹配；用户判断「43 家店不可能每条都记得打，漏打是必然」，
决定**完成情况就按时间猜**。所以：
- 页面上**不再露出 tag**（门店页详情、总部抽屉的话题标签里都过滤掉了），
- `core.js` 的 `match()` **没有精确匹配那一步**，
- `data/scripts.json` 里 `tag` 字段还在（没重新生成数据），但没人用了，别再往 UI 上加。

**「放宽到当周」那一档后来也砍了**（用户：「当周不要，标签其实你猜不准」）。现在只剩一条规则：

1. **同一天**：同一抖音号、发布日 == 计划日、视频未被占用 → 状态 `done`「已发布」墨绿。
2. 过了计划日还没配上 = `late` 逾期砖红；日子还没到 = `todo` 待拍浅灰。
3. 没配上任何脚本的视频 = **自由发挥**，按门店 + 按天统计，在格子右上角蓝点里看链接。

一条视频只匹配一个脚本，一个脚本只取一条视频。
**代价（用户知情并接受）**：店员当天随手发的自由发挥视频会被算成脚本完成，完成率偏乐观。
周次统一以**周一为一周开始**。

## 效果统计

对比「按脚本拍」vs「自由发挥」，两块：

**① 总览表**：视频数 / 平均播放 / **中位播放** / 最高播放 / 总成交价值 / 篇均成交，
下面跟一句人话结论（`.verdict` 浅灰块）。两边样本都要 ≥ `FEW`(5) 条才给结论，不够就直说「先别下结论」。

**② 拆开看**（`#effDimCard`）：三个方框按钮切维度 —— **按门店 / 按内容方向 / 按拍摄形式**，
右边还有个区域经理下拉（`#eMgr`，走同一套 `.xsel`）。
- 门店表统计**全部视频**（脚本 + 自由，另给两列拆分）；内容方向 / 拍摄形式**只统计脚本视频**
  （自由发挥没有 topic/format），这句话写在 `#effDimNote` 上，切维度时自动换。
- `topic`、`format` 都在 `scripts-index.js` 里，**不用加载分片**。
- 点表头排序（`th.s`，`EFF.sort/EFF.desc`）；切维度时排序键回到「平均播放降序」。
- 最后一列是平均播放的对比条，只为一眼看长短，数字还在左边那列。
- 不到 5 条的行**整行压灰 + 标「样本 N 条」**，并在脚注说明别拿它排优劣。

**口径陷阱（两个，都必须留着）**：
1. 播放数只统计导出的「数据日期范围」内，范围外老视频大量为 0 —— 所以先按发布时间筛
   （`effVideos()`，范围取所有导入文件 `数据日期范围` 的并集）。
2. 平均值会被一条爆款整个带飞（实测假数据里「毛孔粗大」均值 3187 / 中位 1779 / 最高 22106），
   所以**每行都同时给中位数**，结论里两个方向不一致时明说「以中位数为准」。

## 本地验证（不用开浏览器）

**无头 Chrome 在这个项目上是死路**（`--dump-dom` 什么都不吐、截图也不准，2026-09-17 和 09-20 各试过一次）。
改完要自己验，走 jsdom：

```bash
npm install --prefix /tmp/jsdomtest jsdom     # 装一次
python3 -m http.server 8765                   # file:// 下 jsdom 不给 localStorage，必须走 http
node tools/fake_videos.js > /tmp/fakevideos.json          # 造假视频数据（真数据可能本机没有）
node tools/dom_check.js '#view=effect' '#p-effect' --seed=/tmp/fakevideos.json
```

`tools/dom_check.js` 会把页面真跑一遍，打印报错 + 指定选择器的文字，还能 `--click=<选择器>`
先点几下（切维度、点表头排序都能这么验）。**它只验逻辑和文字，验不了样式**，配色/排版还是得让用户自己看。

## 发布到线上

GitHub Pages：**https://hejun-coderlife.github.io/hymn-douyin-board/**（仓库 `Hejun-Coderlife/hymn-douyin-board`，公开）。
纯静态，**没有任何服务器要跑**；43 家店同时开也只是 GitHub 在发文件。

换了脚本数据之后的发布流程 = **双击根目录的 `发布到线上.command`**，它做四件事：
`sync_data.py` 重建索引和分片 → 列出变化 → commit + push → 等 Pages 构建完成并打印地址。

手动版就是：
```bash
python3 tools/sync_data.py && git add -A && git commit -m "更新脚本数据" && git push
```

要点：
- `sync_data.py` 是**确定性输出**（无时间戳、键序固定），没变的分片重新生成后内容一致，
  git 认不出改动 → **每次只提交真正变了的月份**，仓库不会每次胖 36MB。
- **`data/scripts.json`（40MB）和 `reference/` 不进仓库**（见 .gitignore）：前者站点不加载、每次整文件替换会撑爆仓库；
  后者那份样例 xlsx 里有视频成交金额。**这也意味着 scripts.json 只存在你本机，要另外备份。**
- **视频数据不会随着上线**：它在各自浏览器的 localStorage 里，线上（github.io）和本地（file://）是两套存储。
  线上要有数据，得在本机「导出备份 JSON」再到线上「导入备份」。
- 国内访问 github.io 偶尔不稳；真成问题就把同一套文件搬到国内静态托管，代码不用改。

## 约定

- 改完网页不要自动 `open` 预览，用户自己刷新。
- **用户是双击 `index.html` 打开的（file://）**，所以数据必须走 `<script src="data/*.js">` 挂全局变量，
  不能用 `fetch('data/*.json')` —— file:// 下 fetch 一律失败，页面会白掉、所有开关没反应。
  `core.js` 里 `loadData()` 优先读 `window.HY_STORES / HY_SCRIPTS`，取不到才退回 fetch。
  **新增任何数据文件都要照这个办法走。**
- localStorage 按来源隔离：`file://` 和 `http://localhost:8765` 各存各的视频数据，互相看不见。
  换打开方式要用「导出备份 / 导入备份 JSON」搬。
- **左侧栏是 `position:fixed` + 纯白不透明**，不能改成毛玻璃：背景那些 3D 球飘过去会透出来，
  看着就像整条栏在动（2026-09-16 用户提「固定住，不允许动」）。
  顶栏后来整条删掉了（`.topbar` 的 CSS 还留着但没人用）。
- 筛选器 = **区域经理（单选）+ 门店（多选）+ 搜索 + 只看有逾期**。
  品牌线筛选 2026-09-17 换成了区域经理；**区域和门店类型两个下拉用户明确说不要**，别再加回去。
- **所有下拉都是自己写的 `.xsel`，不用原生 `<select>`**（用户 2026-09-17：「所有这种下拉标签，都自己设计」）：
  原生 select 藏在 `.xsel` 里当状态，外面是「按钮 + 菜单」，菜单 `position:fixed`（表格 `overflow:auto` 会裁掉 absolute 的），
  开的时候 `placeMenu()` 按按钮位置摆、下方空间不够就往上开，滚动/改窗口大小自动收。
  多选看 `sel.multiple`：选项前面一个小方块，点了不关菜单，按钮上显示「已选 N 家」。
  表格格子里的用 `.xsel.mini`（30px 高）。改完值要手动 `dispatchEvent(new Event('change'))`。
- **区域经理**：名单存 `localStorage.hymn_managers_v1`（跟门店分配分开，"加了名字还没分配门店"也留得住），
  门店归属存在 `stores[].manager`（走 StoreEdits，**记得 `EDITABLE` 里要有 `manager`**，漏了就存不住）。
  两个入口都能管：筛选下拉菜单底部「＋ 新增 / 管理区域经理」、门店列表页管理条的「分配门店…」，
  打开的是 `#mgrModal`——左边名单增删、右边勾这个经理负责哪些店（一家店只能属于一个经理）。
- **搜索框要防中文输入法**：`compositionstart/end` 期间不筛，再加 180ms 防抖；
  否则打拼音打到一半（"qn"）整页就跟着重画（2026-09-17 踩过）。
- 图块区的**柱状图、状态构成条都要能悬停看数字**：不用原生 `<title>`（要等 1 秒、字还小），
  用 `bindSparkHover()` 自己画黑标签；柱状图另外叠了一层透明热区，不然 9px 宽的柱子鼠标瞄不准。
  柱子悬停色是**驼黄 `#B58B3E`**（先试黑、再试深绿，都被否）。
- 面板路由用 `#view=xxx`，**不能直接用 `#board`**：页面里有 `id="board"` 的表格，
  浏览器会跳着滚过去（2026-09-16 踩过）。
- ~~按钮/卡片要有立体层次（渐变底 + inset 高光 + 投影 + hover 抬起）~~ —— 这条是旧画风时期的，
  2026-09-17 改成爱马仕风之后作废：现在就是直角 + 发丝线 + 纯色，层次靠留白和线，不靠阴影。
- 推 GitHub 等用户说「推送」再推。
